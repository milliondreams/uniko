# Cargo configuration.
#
# No build settings are forced here so that a fresh `git clone` builds with
# the standard system linker on every platform (CI and external contributors
# included).
#
# Optional local speed-up: install `mold` and uncomment the block below to
# use it as the link backend (saves ~20-60s on a cold link of the workspace):
#
# [target.'cfg(target_os = "linux")']
# rustflags = ["-C", "link-arg=-fuse-ld=mold"]
#
# Building the `gpu-cuda` feature on a machine with i386 multilib may pick up a
# 32-bit `libcuda.so` under /usr/lib and fail with "incompatible with
# elf64-x86-64". If you hit that, point `linker` at a small `cc` wrapper that
# drops `-L /usr/lib` from argv. See CONTRIBUTING.md for details.
